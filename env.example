# 应用环境
NODE_ENV=production

# 数据库配置（SQLite）
# Docker 环境使用绝对路径，本地开发可以使用相对路径
# DATABASE_URL=file:./data/database.db  # 本地开发
DATABASE_URL=file:/app/data/database.db  # Docker 环境

# JWT 配置
JWT_SECRET=change-this-to-a-random-secret-key-min-32-chars
JWT_EXPIRES_IN=7d

# 默认管理员账户（首次启动时自动创建）
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123456

# 服务端口（主机端口:容器端口）
# BACKEND_PORT 是主机端口，映射到容器内的 3000 端口
# FRONTEND_PORT 是主机端口，映射到容器内的 5173 端口
BACKEND_PORT=3000
FRONTEND_PORT=5173

# CORS 配置（必须与 FRONTEND_PORT 一致）
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# 前端环境变量（必须与 BACKEND_PORT 一致）
VITE_API_URL=http://localhost:3000


